export enum Side {
  PUMP = 'PUMP',
  DUMP = 'DUMP',
}

export enum State {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
  CANCELED = 'CANCELED',
}

export enum TurnResult {
  WIN = 'WIN',
  MISS = 'MISS',
}

export enum SortBy {
  ASC = 'ASC',
  DESC = 'DESC',
}
